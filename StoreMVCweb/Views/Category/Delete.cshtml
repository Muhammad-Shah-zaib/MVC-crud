@model Category

<form method="post" asp-action="Delete">
    <div class="border bg-primary p-3 mt-4">
        <div class="row p-2">
            <h2 class="text-secondary">Delete Category</h2>
            <hr />
        </div>

        <div class="row mt-2">
            <div class="text-danger" asp-validation-for="CustomError"></div>
            <input hidden asp-for="Id"/>
            @* Nameinput*@
            <input hidden asp-for="Name" placeholder="Name..." class="form-control bg-dark text-light my-1" />
            <span class="text-danger" asp-validation-for="Name"></span>

            @* Display order input*@
            <input hidden asp-for="DisplayOrder" placeholder="Name..." class="form-control bg-dark text-light my-1" />
            <span class="text-danger" asp-validation-for="DisplayOrder"></span>
        </div>

        <button type="submit" class="btn btn-outline-danger" style="width: 150px;">Delete</button>
        <a asp-controller="Category" asp-action="Index" class="btn btn-info" style="width: 150px;">Back to list</a>
    </div>
</form>


@* Adding validation script for client side validations *@
@section Scripts {
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}